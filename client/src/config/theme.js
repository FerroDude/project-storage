import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#FFB76B',
      text: '#FFB76B'
    },
    secondary: {
      main: '#e20032'
    }
  }
});

export default theme;
