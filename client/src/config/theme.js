import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#FFB76B'
    },
    secondary: {
      main: '#FFFFFF'
    }
  }
});

export default theme;
